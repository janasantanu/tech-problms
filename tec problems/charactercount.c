#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main(){
    // int n,count = 0;
    // printf("Enter the size of the string: ");
    // scanf("%d",&n);
    // char a[n];
    
    // fgets(a,n,stdin);
    // for(int i = 0;i != '\0';i++){
    //     count++;
    // }
    // printf("The no of characters present in %s is: %d",a,count);
    char a[] = "fkjgdsjfg";
    int y = strlen(a);
    printf("The length is: %d ",y);
}
